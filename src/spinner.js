import { GridLoader } from 'react-spinners';

function Spinner() {
    return (
        <GridLoader className="spinner"
        color="rgba(214, 68, 54, 1)"
        size={25}
      />
    )
}

export default Spinner;